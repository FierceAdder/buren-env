# Exact versions from requirements-frozen.txt (uv export).
# On Colab: do NOT install torch from here — Colab ships CUDA torch; reinstalling breaks GPU.
# Unsloth is installed separately via --no-deps from git (see colab_train.ipynb cell 1).
protobuf==7.34.1
openenv-core==0.2.3
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.3
trl==0.29.1
transformers==4.56.2
torch==2.11.0
huggingface-hub==0.36.2
matplotlib==3.10.9
numpy==2.4.4
requests==2.33.1
datasets==3.6.0
sentencepiece==0.2.1
accelerate==1.13.0
peft==0.19.1
bitsandbytes==0.49.2
# Unsloth is NOT pinned here because openenv-core pulls in unsloth==0.3.1 as a dependency,
# causing a conflict with the newer unsloth==2025.x. Install unsloth separately:
#   Local:  uv sync (uses requirements-frozen.txt which resolves this correctly)
#   Colab:  see colab_train.ipynb cell 1 — installs via --no-deps from git HEAD
unsloth==0.3.1
